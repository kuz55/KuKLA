# RBAC

ADMIN: system administration, users, searches, tasks.
LEADER: operational control, searches, tasks, people.
COORDINATOR: operational control during searches, tasks, people.
SEARCHER: join searches, view operational data, update assigned/available task status, send GPS.
VIEWER: read-only access.

The API enforces roles. UI hiding is not a security boundary.
