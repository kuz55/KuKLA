name: KuKLA CI

# push: ['**'] — пайплайн гоняется на любой ветке, включая feature/* и qa/*.
# Это нужно, чтобы каждую ветку плана видно было зелёной/красной до мержа в develop.
# pull_request оставлен только для main/develop: иначе на ветку с открытым PR
# приходилось бы по два запуска на каждый коммит (push + pull_request).
on:
  push:
    branches: ['**']
    tags-ignore: ['**']
  pull_request:
    branches: [main, develop]

jobs:
  server:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: kukla
          POSTGRES_USER: kukla
          POSTGRES_PASSWORD: kukla
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U kukla -d kukla"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: server/package-lock.json
      - run: npm ci
      - run: npm run build
      - run: npm run migrate
        env:
          DATABASE_URL: postgres://kukla:kukla@localhost:5432/kukla
      # smoke-тесты проверяют живое приложение, поэтому сервер поднимается здесь же,
      # на том же runner'е: Postgres уже доступен как service на localhost:5432.
      - name: Start server
        env:
          DATABASE_URL: postgres://kukla:kukla@localhost:5432/kukla
          JWT_SECRET: ci-only-secret-not-for-production
          PORT: '8080'
        run: nohup node dist/index.js > server.log 2>&1 &
      - name: Wait for server
        run: |
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:8080/health > /dev/null; then
              echo "server is up after ${i}s"; exit 0
            fi
            sleep 1
          done
          echo "server did not become ready in 30s" >&2
          exit 1
      - run: npm test
        env:
          KUKLA_TEST_URL: http://127.0.0.1:8080
      - name: Show server log
        if: failure()
        run: cat server.log

  desktop:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: desktop
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: desktop/package-lock.json
      - run: npm ci
      - run: npm run check
      - run: npm run build
